trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: Maven@3
  inputs:
    mavenPomFile: 'backend/pom.xml'
    goals: 'package'
  workingDirectory: 'backend'

- path: 
    exclude:
    - 'frontend'
